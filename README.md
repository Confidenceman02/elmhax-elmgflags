# elmhax-elmgflags

## Install packages

```bash
pnpm install
```

## Run

```bash
pnpm parcel index.html
```

> [!NOTE]
> You will get a CORS error when making the request to the ASX api. To get around this install a browser extension that allows you to bypass the CORS error.
> I use CORS unblock
